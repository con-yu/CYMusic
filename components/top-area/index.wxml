<!--components/top-area/index.wxml-->
<wxs src="/utils/format.wxs" module="format"></wxs>
<view class="item">
  <view class="album">
    <image class="image" src="{{item.coverImgUrl}}" mode="widthFix"></image>
    <view class="play-counter">{{format.formatCount(item.playCount)}}</view>
  </view>
</view>